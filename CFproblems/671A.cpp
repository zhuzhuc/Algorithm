#include <bits/stdc++.h>
using namespace std;

#define ll long long

typedef unsigned long long ull;
ull a[501] = {1};

void solve() {
    int n;
    cin >> n;
    
    int steps = (n + 4) / 5; 
    cout << steps << endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    solve();

    return 0;
}
